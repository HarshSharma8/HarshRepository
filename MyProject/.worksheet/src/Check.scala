/*class Person {
  var name: String
  var age: Int
  var city: String
}

class Group {
  var field: String
  var knolId: String
}

class PersonService {
  def addManager(employee: Person, newManager: Person)
  def removeManager(employee: Person, oldManager: Person)
  def getGroupByManager(manager: Person): Group
}

class GroupMembershipService {
  def addMemberToGroup(employee: Person, group: Group)
  def removeMemberToGroup(employee: Person, group: Group)
}*/

object Check {
}
